export const APP_NAME = "Workflow"

export const PAGE_PATH = "pages"

export const PROJECT_PATH = "projects"

export const DEFAULT_SIZE = {
  WIDTH: 1200,
  HEIGHT: 900
} as const;

export const WINDOWS = {
  INDEX: "index.html",
  NEW_PROJECT: "new_project.html",
  SELECT_PROJECT: "select_project.html",
} as const;
